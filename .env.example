# ─── Database (local Postgres via docker-compose) ───
DATABASE_URL=postgres://tas:tas_local_dev_password@localhost:5433/tas_dev

# ─── App ───
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="TAS System"

# ─── Auth (Phase 2) ───
# Сгенерировать: openssl rand -base64 32
SESSION_SECRET=replace-me-with-32-bytes-of-random

# ─── Sentry (Phase 0/7) ───
# SENTRY_DSN=
# SENTRY_AUTH_TOKEN=

# ─── PostHog (Phase 0/7) ───
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
